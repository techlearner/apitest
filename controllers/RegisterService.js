'use strict';

exports.registrationManagerRegisterbusinessPost = function(entityId, entityType, firstName, address, city, state, country, pincode, publicKey) {

  var examples = {};
  
  examples['application/json'] = "{}";
  

  
  if(Object.keys(examples).length > 0)
    return examples[Object.keys(examples)[0]];
  
}
